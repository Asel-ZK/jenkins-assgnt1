apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: asel-claim
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests: 
      storage: 5Gi
  storageClassName: norm
